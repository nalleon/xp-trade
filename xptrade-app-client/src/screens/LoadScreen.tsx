import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const LoadScreen = (props: Props) => {
  return (
    <View>
      <Text>Loading...</Text>
    </View>
  )
}

export default LoadScreen

const styles = StyleSheet.create({})
